---
apiVersion: v1
kind: Service
metadata:
  name: alertmanager-ntfy-svc
  labels:
    app: alertmanager-ntfy

spec:
  type: ClusterIP

  selector:
    app: alertmanager-ntfy

  ports:
    - name: http-svc
      protocol: TCP
      port: 8080
      targetPort: http-internal
