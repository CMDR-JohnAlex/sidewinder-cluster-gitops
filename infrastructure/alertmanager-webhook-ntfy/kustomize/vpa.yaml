---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler

metadata:
  name: alertmanager-webhook-ntfy-vpa

spec:
  targetRef:
    apiVersion: "apps/v1"
    kind: Deployment
    name: alertmanager-webhook-ntfy-deployment

  updatePolicy:
    updateMode: "Off"
