---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: immich-https

spec:
  hostnames:
    - immich.sidewinder.lan

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: immich-http

spec:
  hostnames:
    - immich.sidewinder.lan
