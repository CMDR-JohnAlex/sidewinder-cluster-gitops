---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: navidrome-data

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: nfs-client

  resources:
    requests:
      storage: 5Mi
